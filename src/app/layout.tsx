import ClientLayout from './clientLayout'; 
import 'bulma/css/bulma.css'; // caminho para o arquivo

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <ClientLayout>{children}</ClientLayout>
      </body>
    </html>
  );
}
